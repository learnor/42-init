# Name the command that determines the state of the SSH service.

service ssh status

# Don't forget to sudo
# Make sure your ssh is installed
# 'apt-get update'
# 'apt-get install openssh-server'
# Both need sudo
